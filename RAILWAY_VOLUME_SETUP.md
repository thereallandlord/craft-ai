# Настройка Railway Volume для Carousel Generator v7.0

## Проблема

По умолчанию Railway использует **эфемерное хранилище** - все файлы, созданные во время работы приложения, **удаляются при каждом новом деплое**. Это значит, что все шаблоны, загруженные шрифты и сгенерированные изображения теряются при обновлении приложения.

## Решение: Railway Volume

**Railway Volume** - это постоянное облачное хранилище, которое сохраняется между деплоями. Данные в Volume не удаляются при обновлениях приложения.

---

## Шаг 1: Создание Volume в Railway

1. Открой свой проект в Railway Dashboard
2. Перейди в настройки сервиса (Settings)
3. Найди секцию **"Volumes"**
4. Нажми **"New Volume"**
5. Заполни параметры:
   - **Name**: `carousel-data` (или любое другое название)
   - **Mount Path**: `/app/data` (обязательно!)
   - **Size**: минимум 1 GB (рекомендую 5 GB для большого количества шаблонов)
6. Нажми **"Create Volume"**

---

## Шаг 2: Настройка переменной окружения

После создания Volume нужно указать приложению, где находится папка для данных:

1. В Railway Dashboard открой вкладку **"Variables"**
2. Добавь новую переменную:
   - **Key**: `DATA_PATH`
   - **Value**: `/app/data`
3. Сохрани изменения
4. Railway автоматически перезапустит приложение

---

## Шаг 3: Проверка работы

После перезапуска приложения:

1. Открой логи сервиса (Logs в Railway Dashboard)
2. Найди строки:
   ```
   📁 Templates directory: /app/data/templates
   📁 Uploads directory: /app/data/uploads
   📁 Output directory: /app/data/output
   ```
3. Если видишь эти строки - Volume настроен правильно!

---

## Что хранится в Volume?

После настройки в `/app/data` будут храниться:

- **templates/** - все шаблоны (kamalov.json, MargulanGames.json и т.д.)
- **uploads/** - загруженные пользователем шрифты
- **output/** - сгенерированные карусели (PNG файлы)

---

## Проверка работы через API

Создай тестовый шаблон через API:

```bash
curl -X POST https://your-app.up.railway.app/templates \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test Railway Volume",
    "template_id": "test_volume",
    "settings": {"username": "@test"},
    "slides": [{"id": "1", "type": "intro"}]
  }'
```

Теперь:
1. Сделай новый деплой (push изменений в Git или вручную redeploy)
2. После перезапуска проверь список шаблонов:
   ```bash
   curl https://your-app.up.railway.app/templates
   ```
3. Если `test_volume` всё ещё есть в списке - Volume работает корректно!

---

## Миграция существующих шаблонов

Если у тебя уже есть шаблоны в проекте (например, kamalov.json), их нужно скопировать в Volume вручную:

### Вариант 1: Через Railway Shell
1. В Railway Dashboard открой вкладку **"Shell"**
2. Выполни команды:
   ```bash
   cd /app
   cp templates/*.json /app/data/templates/
   ```

### Вариант 2: Повторная загрузка через API
1. Экспортируй шаблоны локально (через UI или скачай JSON файлы)
2. Загрузи их обратно через POST /templates (они автоматически сохранятся в Volume)

---

## Локальная разработка

Для локального тестирования с Volume:

```bash
# С дефолтным путем (текущая директория)
python main.py

# С кастомным путем (имитация Railway)
DATA_PATH=/tmp/carousel_data python main.py
```

---

## Troubleshooting

### Проблема: "Templates directory: templates" (без /app/data)
**Решение**: Проверь, что переменная `DATA_PATH=/app/data` добавлена в Railway Variables.

### Проблема: Шаблоны всё равно теряются
**Решение**:
1. Проверь, что Volume подключен к сервису (Railway Settings → Volumes)
2. Убедись, что Mount Path = `/app/data`
3. Перезапусти сервис вручную (Redeploy)

### Проблема: Ошибка "Permission denied" при сохранении
**Решение**: Volume может быть read-only. Пересоздай Volume с правами на запись.

---

## Стоимость

Railway Volume:
- Первые 5 GB - **$0.25/месяц**
- Каждые дополнительные 5 GB - **$0.25/месяц**

Для карусель-генератора достаточно 1-5 GB (стоимость ~$0.25-0.50/месяц).

---

## Важно!

После настройки Volume:
- Все новые шаблоны автоматически сохраняются в постоянное хранилище
- Обновления приложения **не удаляют** пользовательские данные
- Backup Volume можно сделать через Railway CLI: `railway volume backup carousel-data`

---

## Проверка успешной настройки - чеклист

- [ ] Volume создан с mount path `/app/data`
- [ ] Переменная `DATA_PATH=/app/data` добавлена
- [ ] В логах видны правильные пути с `/app/data`
- [ ] Тестовый шаблон сохраняется и остается после redeploy
- [ ] Существующие шаблоны (kamalov.json) доступны

Если все пункты отмечены - настройка завершена успешно!
